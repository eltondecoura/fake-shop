name: CI-CD
on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Obtendo o Codigo
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Construcao e envio da imagem docker
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          file: ./src/Dockerfile
          tags: eltondecoura/fake-shop-desafio:v${{ github_run_number }}
          
      - run: echo "Enviar a Imagem Docker para Docker Hub"

  CD:
    needs: [CI]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obter Codigo"
      - run: echo "Configurar o Kubeconfig"
      - run: echo "Executar o apply"


      

      
      
    
